package stage.farouk.controller;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.web.bind.annotation.*;
import stage.farouk.entity.ExeSequence;
import stage.farouk.services.exeIsequenceService;

@CrossOrigin(origins = "*")
@RestController
@RequestMapping("/exeSequence")
public class exeSequenceController {
}
